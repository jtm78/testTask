package core.driverConfiguration;

import org.openqa.selenium.WebDriver;

/**
 * Interface for all types of driver configuration
 */
public interface IDriverConfiguration {
    WebDriver getDriver();
}
